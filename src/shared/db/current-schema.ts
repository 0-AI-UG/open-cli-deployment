import { initializeNtfySchema } from "./ntfy-schema.ts";
import { initializeIncidentHistorySchema } from "./incident-history-schema.ts";
import type { Database } from "bun:sqlite";
import { initializeProtectionSchema } from "./protection-schema.ts";

export const CURRENT_SCHEMA_VERSION = 120;

const CURRENT_SCHEMA_STATEMENTS = [
  "CREATE TABLE action_confirmations (\n        confirm_code TEXT PRIMARY KEY,\n        user_code    TEXT NOT NULL UNIQUE,\n        user_id      TEXT NOT NULL,\n        action       TEXT NOT NULL,\n        summary      TEXT NOT NULL,\n        status       TEXT NOT NULL,\n        expires_at   INTEGER NOT NULL,\n        created_at   INTEGER NOT NULL\n      , resource_type TEXT NOT NULL DEFAULT '', resource_id TEXT NOT NULL DEFAULT '')",
  "CREATE TABLE \"apps\" (\n          id INTEGER PRIMARY KEY AUTOINCREMENT,\n          name TEXT NOT NULL,\n          domain TEXT NOT NULL,\n          container_port INTEGER NOT NULL DEFAULT 3000,\n          env_vars TEXT NOT NULL DEFAULT '{}',\n          status TEXT NOT NULL DEFAULT 'deploying',\n          deploy_log TEXT NOT NULL DEFAULT '',\n          created_at TEXT NOT NULL DEFAULT (datetime('now')),\n          volume_id TEXT NOT NULL DEFAULT '',\n          volume_mount TEXT NOT NULL DEFAULT '',\n          desired_replicas INTEGER NOT NULL DEFAULT 1,\n          min_replicas INTEGER NOT NULL DEFAULT 1,\n          max_replicas INTEGER NOT NULL DEFAULT 1,\n          autoscale_enabled INTEGER NOT NULL DEFAULT 0,\n          autoscale_cpu_threshold INTEGER NOT NULL DEFAULT 80,\n          autoscale_mem_threshold INTEGER NOT NULL DEFAULT 85,\n          autoscale_cooldown INTEGER NOT NULL DEFAULT 300,\n          last_scale_at TEXT,\n          deployed_by TEXT NOT NULL DEFAULT '', sleeping_server_id INTEGER, sleeping_host_port INTEGER, scale_to_zero_after INTEGER NOT NULL DEFAULT 300, environment_id INTEGER REFERENCES environments(id), public INTEGER NOT NULL DEFAULT 1, extra_volumes TEXT NOT NULL DEFAULT '[]', memory_mb INTEGER NOT NULL DEFAULT 0, health_check INTEGER NOT NULL DEFAULT 1, internal_port INTEGER NOT NULL DEFAULT 0, auth_password_hash TEXT NOT NULL DEFAULT '', last_request_at TEXT, requests_per_min REAL NOT NULL DEFAULT 0, sticky INTEGER NOT NULL DEFAULT 0, rate_limit_rps INTEGER NOT NULL DEFAULT 0, ip_allowlist TEXT NOT NULL DEFAULT '', health_check_path TEXT NOT NULL DEFAULT '', compress INTEGER NOT NULL DEFAULT 0, public_port INTEGER, public_protocol TEXT NOT NULL DEFAULT 'tcp', internal_protocol TEXT NOT NULL DEFAULT 'http', cpu_limit REAL NOT NULL DEFAULT 0, autoscale_req_threshold INTEGER NOT NULL DEFAULT 0, stack_id INTEGER, volume_attached INTEGER NOT NULL DEFAULT 0, durability_class TEXT NOT NULL DEFAULT 'none', max_per_host INTEGER NOT NULL DEFAULT 0, min_locations INTEGER NOT NULL DEFAULT 1, placement_pool TEXT NOT NULL DEFAULT 'general', target TEXT NOT NULL DEFAULT '', target_of INTEGER, virtual_ip TEXT NOT NULL DEFAULT '', stack_needs TEXT,  environment_stale INTEGER NOT NULL DEFAULT 0, config_revision INTEGER NOT NULL DEFAULT 1, last_manifest_path TEXT, last_manifest_hash TEXT, last_manifest_applied_at TEXT, last_manifest_config_revision INTEGER, image_ref TEXT NOT NULL DEFAULT '', health_check_mode TEXT NOT NULL DEFAULT 'http', health_check_command TEXT NOT NULL DEFAULT '', health_check_file TEXT NOT NULL DEFAULT '', health_check_max_age_seconds INTEGER NOT NULL DEFAULT 0, public_endpoint_status TEXT NOT NULL DEFAULT 'unknown', public_endpoint_error TEXT NOT NULL DEFAULT '', public_endpoint_checked_at TEXT, rollout_requested_revision INTEGER NOT NULL DEFAULT 0, rollout_requested_after_deployment_id INTEGER NOT NULL DEFAULT 0, deletion_requested_at TEXT, desired_volume_id TEXT NOT NULL DEFAULT '', desired_volume_size INTEGER NOT NULL DEFAULT 0, desired_volume_path TEXT NOT NULL DEFAULT '/data', health_check_expected_statuses TEXT NOT NULL DEFAULT '[200]', manifest_path TEXT, stack_manifest_path TEXT, build_source_id INTEGER REFERENCES build_sources(id) ON DELETE SET NULL, build_repository TEXT NOT NULL DEFAULT '', build_branch TEXT NOT NULL DEFAULT 'main', build_dockerfile TEXT NOT NULL DEFAULT 'Dockerfile', build_context TEXT NOT NULL DEFAULT '.', build_image TEXT NOT NULL DEFAULT '', command_json TEXT NOT NULL DEFAULT '[]' CHECK (json_valid(command_json)), cap_add_json TEXT NOT NULL DEFAULT '[]' CHECK (json_valid(cap_add_json)), post_start_command TEXT NOT NULL DEFAULT '', volume_driver TEXT NOT NULL DEFAULT '', desired_volume_driver TEXT NOT NULL DEFAULT '')",
  "CREATE TABLE availability_samples (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        app_id INTEGER NOT NULL REFERENCES apps(id) ON DELETE CASCADE,\n        meets_target INTEGER NOT NULL,\n        desired_count INTEGER NOT NULL,\n        running_count INTEGER NOT NULL,\n        distinct_hosts INTEGER NOT NULL,\n        distinct_locations INTEGER NOT NULL,\n        sampled_at TEXT NOT NULL DEFAULT (datetime('now'))\n      )",
  "CREATE TABLE build_artifacts (\n        operation_id INTEGER NOT NULL REFERENCES operations(id) ON DELETE CASCADE,\n        target_name TEXT NOT NULL CHECK (length(target_name) > 0),\n        image_ref TEXT NOT NULL CHECK (\n          instr(image_ref, '@sha256:') > 1\n          AND length(substr(image_ref, instr(image_ref, '@sha256:') + 8)) = 64\n          AND substr(image_ref, instr(image_ref, '@sha256:') + 8) NOT GLOB '*[^0-9a-f]*'\n        ),\n        repository TEXT NOT NULL CHECK (length(repository) > 0),\n        commit_sha TEXT NOT NULL CHECK (length(commit_sha) > 0),\n        worker_id INTEGER REFERENCES build_workers(id) ON DELETE SET NULL,\n        verified_at TEXT NOT NULL DEFAULT (datetime('now')),\n        PRIMARY KEY (operation_id, target_name)\n      )",
  "CREATE TABLE build_result_checkpoints (\n        operation_id INTEGER PRIMARY KEY REFERENCES operations(id) ON DELETE CASCADE,\n        repository TEXT NOT NULL CHECK (length(repository) > 0),\n        commit_sha TEXT NOT NULL CHECK (length(commit_sha) > 0),\n        worker_id INTEGER REFERENCES build_workers(id) ON DELETE SET NULL,\n        output_json TEXT NOT NULL CHECK (json_valid(output_json)),\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      )",
  "CREATE TABLE build_source_deliveries (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        source_id INTEGER NOT NULL REFERENCES build_sources(id) ON DELETE CASCADE,\n        delivery_id TEXT NOT NULL CHECK (length(delivery_id) > 0),\n        commit_sha TEXT NOT NULL CHECK (length(commit_sha) > 0),\n        event_at TEXT,\n        received_at TEXT NOT NULL DEFAULT (datetime('now')),\n        operation_id INTEGER REFERENCES operations(id) ON DELETE SET NULL,\n        status TEXT NOT NULL DEFAULT 'received' CHECK (status IN (\n          'received', 'queued', 'building', 'deployed', 'failed',\n          'duplicate', 'stale', 'superseded'\n        )),\n        superseded_by INTEGER REFERENCES build_source_deliveries(id) ON DELETE SET NULL,\n        UNIQUE(source_id, delivery_id)\n      )",
  "CREATE TABLE build_sources (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        repository TEXT NOT NULL,\n        branch TEXT NOT NULL DEFAULT 'main',\n        worker_id INTEGER NOT NULL REFERENCES build_workers(id) ON DELETE RESTRICT,\n        webhook_enabled INTEGER NOT NULL DEFAULT 1,\n        last_delivery_id TEXT NOT NULL DEFAULT '',\n        last_commit TEXT NOT NULL DEFAULT '',\n        last_status TEXT NOT NULL DEFAULT '',\n        last_error TEXT NOT NULL DEFAULT '',\n        last_received_at TEXT,\n        created_at TEXT NOT NULL DEFAULT (datetime('now')),\n        UNIQUE(repository, branch)\n      )",
  "CREATE TABLE build_worker_leases (\n        worker_id INTEGER NOT NULL REFERENCES build_workers(id) ON DELETE RESTRICT,\n        slot INTEGER NOT NULL DEFAULT 0 CHECK (slot >= 0),\n        operation_id INTEGER NOT NULL UNIQUE REFERENCES operations(id) ON DELETE CASCADE,\n        lease_token TEXT NOT NULL UNIQUE,\n        acquired_at TEXT NOT NULL DEFAULT (datetime('now')),\n        heartbeat_at TEXT NOT NULL DEFAULT (datetime('now')),\n        expires_at TEXT NOT NULL,\n        PRIMARY KEY (worker_id, slot)\n      )",
  "CREATE TABLE \"build_workers\" (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        server_id INTEGER NOT NULL UNIQUE REFERENCES servers(id) ON DELETE RESTRICT,\n        name TEXT NOT NULL UNIQUE,\n        architecture TEXT NOT NULL DEFAULT '',\n        previous_pool TEXT NOT NULL DEFAULT 'general',\n        status TEXT NOT NULL DEFAULT 'installing',\n        last_error TEXT NOT NULL DEFAULT '',\n        last_checked_at TEXT,\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , worker_version TEXT NOT NULL DEFAULT '', draining INTEGER NOT NULL DEFAULT 0 CHECK (draining IN (0, 1)), disk_free_bytes INTEGER NOT NULL DEFAULT 0 CHECK (disk_free_bytes >= 0), last_used_at TEXT)",
  "CREATE TABLE deployment_history (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        app_id INTEGER NOT NULL REFERENCES apps(id) ON DELETE CASCADE,\n        image_tag TEXT NOT NULL,\n        git_commit TEXT NOT NULL DEFAULT '',\n        status TEXT NOT NULL DEFAULT 'deployed',\n        deploy_log TEXT NOT NULL DEFAULT '',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , source TEXT NOT NULL DEFAULT 'manual', config_revision INTEGER NOT NULL DEFAULT 1, image_digest TEXT NOT NULL DEFAULT '', env_hash TEXT NOT NULL DEFAULT '', image_size_bytes INTEGER NOT NULL DEFAULT 0, archive_size_bytes INTEGER NOT NULL DEFAULT 0, transfer_size_bytes INTEGER NOT NULL DEFAULT 0, operation_id INTEGER)",
  "CREATE TABLE device_auth_codes (\n        device_code TEXT PRIMARY KEY,\n        user_code TEXT NOT NULL UNIQUE,\n        user_id TEXT,\n        status TEXT NOT NULL,\n        expires_at INTEGER NOT NULL,\n        created_at INTEGER NOT NULL\n      )",
  "CREATE TABLE encrypted_secrets (\n        key TEXT PRIMARY KEY,\n        encrypted_value TEXT NOT NULL,\n        iv TEXT NOT NULL,\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      )",
  "CREATE TABLE \"environments\" (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        name TEXT NOT NULL UNIQUE,\n        env_vars TEXT NOT NULL DEFAULT '{\"version\":2,\"entries\":[]}',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , deleted_at TEXT, purge_after TEXT)",
  "CREATE TABLE metrics_samples (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        replica_id INTEGER NOT NULL REFERENCES replicas(id) ON DELETE CASCADE,\n        app_id INTEGER NOT NULL REFERENCES apps(id) ON DELETE CASCADE,\n        cpu_percent REAL NOT NULL,\n        memory_percent REAL NOT NULL,\n        sampled_at TEXT NOT NULL DEFAULT (datetime('now'))\n      )",
  "CREATE TABLE operation_logs (\n        id      INTEGER PRIMARY KEY AUTOINCREMENT,\n        op_id   INTEGER NOT NULL REFERENCES operations(id) ON DELETE CASCADE,\n        ts      TEXT NOT NULL DEFAULT (datetime('now')),\n        level   TEXT NOT NULL DEFAULT 'info',\n        message TEXT NOT NULL\n      , attempt INTEGER NOT NULL DEFAULT 1)",
  "CREATE TABLE operation_steps (\n        op_id       INTEGER NOT NULL REFERENCES operations(id) ON DELETE CASCADE,\n        seq         INTEGER NOT NULL,\n        step        TEXT NOT NULL,\n        phase       TEXT NOT NULL DEFAULT 'forward',\n        status      TEXT NOT NULL,\n        output_json TEXT,\n        detail      TEXT NOT NULL DEFAULT '',\n        started_at  TEXT NOT NULL DEFAULT (datetime('now')),\n        finished_at TEXT,\n        PRIMARY KEY (op_id, seq)\n      )",
  "CREATE TABLE operations (\n        id              INTEGER PRIMARY KEY AUTOINCREMENT,\n        kind            TEXT NOT NULL,\n        resource_keys   TEXT NOT NULL,\n        input_json      TEXT NOT NULL DEFAULT '{}',\n        status          TEXT NOT NULL DEFAULT 'pending',\n        parent_id       INTEGER REFERENCES operations(id) ON DELETE SET NULL,\n        attempt         INTEGER NOT NULL DEFAULT 0,\n        scheduled_for   TEXT,\n        last_step       TEXT,\n        error_json      TEXT,\n        trigger         TEXT NOT NULL DEFAULT 'ui',\n        triggered_by    TEXT NOT NULL DEFAULT '',\n        idempotency_key TEXT,\n        enqueued_at     TEXT NOT NULL DEFAULT (datetime('now')),\n        started_at      TEXT,\n        finished_at     TEXT\n      )",
  "CREATE TABLE panel (\n        id INTEGER PRIMARY KEY CHECK (id = 1),\n        server_id INTEGER NOT NULL REFERENCES servers(id) ON DELETE CASCADE,\n        name TEXT NOT NULL,\n        domain TEXT NOT NULL,\n        container_port INTEGER NOT NULL,\n        host_port INTEGER NOT NULL,\n        volume_id TEXT NOT NULL DEFAULT '',\n        volume_mount TEXT NOT NULL DEFAULT '',\n        env_vars TEXT NOT NULL DEFAULT '{}',\n        status TEXT NOT NULL DEFAULT 'running',\n        deploy_log TEXT NOT NULL DEFAULT '',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , image_ref TEXT NOT NULL DEFAULT '', volume_driver TEXT NOT NULL DEFAULT '')",
  "CREATE TABLE panel_deployments (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        image_tag TEXT NOT NULL,\n        git_commit TEXT NOT NULL DEFAULT '',\n        status TEXT NOT NULL DEFAULT 'deployed',\n        source TEXT NOT NULL DEFAULT 'manual',\n        deploy_log TEXT NOT NULL DEFAULT '',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      )",
  "CREATE TABLE port_reservations (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        server_id INTEGER NOT NULL REFERENCES servers(id) ON DELETE CASCADE,\n        bind_address TEXT NOT NULL,\n        host_port INTEGER NOT NULL,\n        protocol TEXT NOT NULL DEFAULT 'tcp',\n        owner_type TEXT NOT NULL,\n        owner_id TEXT NOT NULL,\n        created_at TEXT NOT NULL DEFAULT (datetime('now')),\n        UNIQUE(server_id, bind_address, host_port, protocol)\n      )",
  "CREATE TABLE replicas (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        app_id INTEGER NOT NULL REFERENCES apps(id) ON DELETE CASCADE,\n        server_id INTEGER NOT NULL REFERENCES servers(id) ON DELETE CASCADE,\n        host_port INTEGER NOT NULL,\n        container_name TEXT NOT NULL,\n        status TEXT NOT NULL DEFAULT 'deploying',\n        cpu_percent REAL NOT NULL DEFAULT 0,\n        memory_percent REAL NOT NULL DEFAULT 0,\n        last_health_at TEXT,\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , unhealthy_ticks INTEGER NOT NULL DEFAULT 0, stopped_at TEXT, cpu_limit_cores REAL NOT NULL DEFAULT 0, memory_used_mb REAL NOT NULL DEFAULT 0, memory_limit_mb REAL NOT NULL DEFAULT 0, image_digest TEXT NOT NULL DEFAULT '', desired_image_digest TEXT NOT NULL DEFAULT '', env_hash TEXT NOT NULL DEFAULT '', config_revision INTEGER NOT NULL DEFAULT 0, attested_at TEXT, attestation_error TEXT NOT NULL DEFAULT '')",
  "CREATE TABLE retired_volumes (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        provider_volume_id TEXT NOT NULL UNIQUE,\n        former_resource_type TEXT NOT NULL,\n        former_resource_id INTEGER NOT NULL,\n        former_resource_name TEXT NOT NULL,\n        reason TEXT NOT NULL,\n        state TEXT NOT NULL DEFAULT 'detached',\n        retired_at TEXT NOT NULL DEFAULT (datetime('now')),\n        purge_after TEXT NOT NULL DEFAULT (datetime('now', '+7 days'))\n      , retention_class TEXT NOT NULL DEFAULT 'user', driver_id TEXT NOT NULL DEFAULT '')",
  "CREATE TABLE scaling_events (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        app_id INTEGER NOT NULL REFERENCES apps(id) ON DELETE CASCADE,\n        event_type TEXT NOT NULL,\n        from_count INTEGER NOT NULL,\n        to_count INTEGER NOT NULL,\n        reason TEXT NOT NULL DEFAULT '',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , operation_id INTEGER)",
  "CREATE TABLE schema_version (\n    version INTEGER NOT NULL DEFAULT 0\n  )",
  "CREATE TABLE server_metrics_samples (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        server_id INTEGER NOT NULL REFERENCES servers(id) ON DELETE CASCADE,\n        cpu_percent REAL NOT NULL,\n        memory_percent REAL NOT NULL,\n        sampled_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , disk_used_gb REAL NOT NULL DEFAULT 0, disk_total_gb REAL NOT NULL DEFAULT 0)",
  "CREATE TABLE \"servers\" (\n          id INTEGER PRIMARY KEY AUTOINCREMENT,\n          name TEXT NOT NULL,\n          provider_id TEXT NOT NULL DEFAULT '',\n          ipv4 TEXT NOT NULL DEFAULT '',\n          ipv6 TEXT NOT NULL DEFAULT '',\n          type TEXT NOT NULL DEFAULT 'cx23',\n          location TEXT NOT NULL DEFAULT 'nbg1',\n          status TEXT NOT NULL DEFAULT 'provisioning',\n          ssh_host_key TEXT NOT NULL DEFAULT '',\n          routing_address TEXT NOT NULL DEFAULT '',\n          created_at TEXT NOT NULL DEFAULT (datetime('now'))\n        , pool TEXT NOT NULL DEFAULT 'general', provider_status TEXT NOT NULL DEFAULT '', last_observed_at TEXT, unavailable_ticks INTEGER NOT NULL DEFAULT 0, gc_requested_at TEXT, provider TEXT NOT NULL DEFAULT 'external', ownership TEXT NOT NULL DEFAULT 'connected', management_address TEXT NOT NULL DEFAULT '', ssh_user TEXT NOT NULL DEFAULT 'root', ssh_port INTEGER NOT NULL DEFAULT 22)",
  "CREATE TABLE settings (\n    key TEXT PRIMARY KEY,\n    value TEXT NOT NULL DEFAULT ''\n  )",
  "CREATE TABLE stacks (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        name TEXT UNIQUE NOT NULL,\n        environment_id INTEGER,\n        status TEXT NOT NULL DEFAULT 'deploying',\n        deploy_log TEXT NOT NULL DEFAULT '',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , staging_environment_id INTEGER, staging_env_keys TEXT NOT NULL DEFAULT '[]')",
  "CREATE TABLE \"user_permissions\" (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n        permission TEXT NOT NULL,\n        scope_type TEXT NOT NULL DEFAULT 'global',\n        scope_id TEXT,\n        UNIQUE(user_id, permission, scope_type, scope_id)\n      )",
  "CREATE TABLE \"users\" (\n        id TEXT PRIMARY KEY,\n        username TEXT NOT NULL UNIQUE,\n        password_hash TEXT NOT NULL,\n        is_admin INTEGER NOT NULL DEFAULT 0,\n        webauthn_enabled INTEGER NOT NULL DEFAULT 0,\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      , github_id INTEGER, github_username TEXT NOT NULL DEFAULT '', github_avatar_url TEXT NOT NULL DEFAULT '', github_linked_at TEXT, token_version INTEGER NOT NULL DEFAULT 0)",
  "CREATE TABLE volume_deletion_audit (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        actor_user_id TEXT NOT NULL,\n        provider_volume_id TEXT NOT NULL,\n        provider_volume_name TEXT NOT NULL,\n        former_resource_type TEXT NOT NULL DEFAULT '',\n        former_resource_id INTEGER NOT NULL DEFAULT 0,\n        former_resource_name TEXT NOT NULL DEFAULT '',\n        retention_state TEXT NOT NULL DEFAULT '',\n        retired_at TEXT,\n        purge_after TEXT,\n        status TEXT NOT NULL DEFAULT 'pending',\n        error TEXT NOT NULL DEFAULT '',\n        requested_at TEXT NOT NULL DEFAULT (datetime('now')),\n        completed_at TEXT\n      , driver_id TEXT NOT NULL DEFAULT '')",
  "CREATE TABLE webauthn_challenges (\n        id TEXT PRIMARY KEY,\n        user_id TEXT NOT NULL,\n        challenge TEXT NOT NULL,\n        kind TEXT NOT NULL,\n        expires_at INTEGER NOT NULL,\n        created_at INTEGER NOT NULL\n      )",
  "CREATE TABLE webauthn_credentials (\n        id TEXT PRIMARY KEY,\n        user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n        public_key BLOB NOT NULL,\n        counter INTEGER NOT NULL DEFAULT 0,\n        device_type TEXT NOT NULL DEFAULT '',\n        backed_up INTEGER NOT NULL DEFAULT 0,\n        transports TEXT NOT NULL DEFAULT '[]',\n        name TEXT NOT NULL DEFAULT 'Passkey',\n        created_at TEXT NOT NULL DEFAULT (datetime('now'))\n      )",
  "CREATE INDEX build_artifacts_source ON build_artifacts(repository, commit_sha)",
  "CREATE INDEX build_artifacts_worker ON build_artifacts(worker_id)",
  "CREATE INDEX build_result_checkpoints_source ON build_result_checkpoints(repository, commit_sha)",
  "CREATE INDEX build_result_checkpoints_worker ON build_result_checkpoints(worker_id)",
  "CREATE INDEX build_source_deliveries_operation\n        ON build_source_deliveries(operation_id) WHERE operation_id IS NOT NULL",
  "CREATE INDEX build_source_deliveries_source_event\n        ON build_source_deliveries(source_id, event_at DESC, received_at DESC, id DESC)",
  "CREATE INDEX build_source_deliveries_source_status\n        ON build_source_deliveries(source_id, status, event_at DESC, id DESC)",
  "CREATE INDEX build_worker_leases_expiry ON build_worker_leases(expires_at)",
  "CREATE UNIQUE INDEX deployment_history_operation_id_unique ON deployment_history(operation_id) WHERE operation_id IS NOT NULL",
  "CREATE INDEX idx_action_confirmations_expires ON action_confirmations(expires_at)",
  "CREATE INDEX idx_action_confirmations_user_code ON action_confirmations(user_code)",
  "CREATE UNIQUE INDEX idx_apps_internal_port ON apps(internal_port) WHERE internal_port > 0",
  "CREATE UNIQUE INDEX idx_apps_public_port ON apps(public_port) WHERE public_port IS NOT NULL",
  "CREATE UNIQUE INDEX idx_apps_virtual_ip ON apps(virtual_ip) WHERE virtual_ip != ''",
  "CREATE INDEX idx_availability_samples_app ON availability_samples(app_id, sampled_at)",
  "CREATE INDEX idx_device_auth_codes_expires ON device_auth_codes(expires_at)",
  "CREATE INDEX idx_device_auth_codes_user_code ON device_auth_codes(user_code)",
  "CREATE INDEX idx_environments_deleted_at ON environments(deleted_at)",
  "CREATE INDEX idx_metrics_samples_app_time ON metrics_samples(app_id, sampled_at)",
  "CREATE INDEX idx_metrics_samples_replica_time ON metrics_samples(replica_id, sampled_at)",
  "CREATE INDEX idx_port_reservations_owner ON port_reservations(owner_type, owner_id)",
  "CREATE INDEX idx_server_metrics_server_time ON server_metrics_samples(server_id, sampled_at)",
  "CREATE UNIQUE INDEX idx_users_github_id ON users(github_id) WHERE github_id IS NOT NULL",
  "CREATE INDEX idx_volume_deletion_audit_provider ON volume_deletion_audit(provider_volume_id, requested_at)",
  "CREATE INDEX idx_webauthn_challenges_expires ON webauthn_challenges(expires_at)",
  "CREATE INDEX idx_webauthn_challenges_user_kind ON webauthn_challenges(user_id, kind)",
  "CREATE INDEX idx_webauthn_user ON webauthn_credentials(user_id)",
  "CREATE INDEX op_logs_op_id ON operation_logs(op_id, id)",
  "CREATE UNIQUE INDEX ops_idempotency ON operations(idempotency_key) WHERE idempotency_key IS NOT NULL",
  "CREATE INDEX ops_parent ON operations(parent_id)",
  "CREATE INDEX ops_status_sched ON operations(status, scheduled_for)",
  "CREATE UNIQUE INDEX scaling_events_operation_id_unique ON scaling_events(operation_id) WHERE operation_id IS NOT NULL",
  "CREATE TRIGGER apps_bump_config_revision\n        AFTER UPDATE OF\n          domain, image_ref, container_port, auth_password_hash,\n          environment_id, public, health_check,\n          health_check_mode, health_check_command, health_check_file,\n          health_check_max_age_seconds, health_check_expected_statuses,\n          internal_protocol, sticky, rate_limit_rps, ip_allowlist,\n          health_check_path, compress, public_port, public_protocol,\n          desired_replicas, min_replicas, max_replicas, autoscale_enabled,\n          autoscale_cpu_threshold, autoscale_mem_threshold,\n          autoscale_cooldown, autoscale_req_threshold, scale_to_zero_after,\n          desired_volume_id, desired_volume_size, desired_volume_path,\n          extra_volumes, memory_mb, cpu_limit, durability_class,\n          max_per_host, min_locations, placement_pool\n        ON apps\n        BEGIN\n          UPDATE apps SET\n            config_revision = config_revision + 1,\n            rollout_requested_revision = CASE\n              WHEN rollout_requested_revision > 0 THEN config_revision + 1\n              ELSE 0\n            END\n          WHERE id = NEW.id;\n        END",
  "CREATE TRIGGER build_artifacts_image_ref_immutable\n        BEFORE UPDATE OF image_ref ON build_artifacts\n        WHEN OLD.image_ref <> NEW.image_ref\n        BEGIN\n          SELECT RAISE(ABORT, 'build artifact image_ref is immutable');\n        END",
] as const;

const DEFAULT_SETTINGS = [
  {
    "key": "default_domain_suffix",
    "value": ""
  },
  {
    "key": "default_location",
    "value": ""
  },
  {
    "key": "default_server_type",
    "value": ""
  },
  {
    "key": "require_2fa",
    "value": "1"
  },
  {
    "key": "ssh_public_key",
    "value": ""
  }
] as const;

/** Initialize a brand-new database directly at the current schema. */
export function initializeCurrentSchema(db: Database): void {
  db.run("BEGIN TRANSACTION");
  try {
    for (const statement of CURRENT_SCHEMA_STATEMENTS) db.run(statement);
    initializeProtectionSchema(db);
    initializeNtfySchema(db);
    initializeIncidentHistorySchema(db);
    db.run("INSERT INTO schema_version (version) VALUES (?)", [CURRENT_SCHEMA_VERSION]);
    const insertSetting = db.prepare("INSERT INTO settings (key, value) VALUES (?, ?)");
    for (const setting of DEFAULT_SETTINGS) insertSetting.run(setting.key, setting.value);
    db.run("COMMIT");
  } catch (error) {
    db.run("ROLLBACK");
    throw error;
  }
}
